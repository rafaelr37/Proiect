#include <stdio.h> 

int main(void)
{
    int nr, rom;

    printf("Introdu o valoare: ");
    scanf("%d", &nr);

    printf("In cifre romane: : ");

    while (nr != 0)
    {
        if (num >= 1000) 
        {
            printf("M");
            num -= 1000;
        }


    }

    return 0;
}